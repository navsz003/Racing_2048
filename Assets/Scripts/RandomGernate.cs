using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Racing
{
    public class RandomGernate : MonoBehaviour
    {
        float minInterval;
        float maxInterval;
        Vector2 generatePos;

        // Start is called before the first frame update
        void Start()
        {
            //generatePos = new Vector2(Random.Range(-1f, 1f));
        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}